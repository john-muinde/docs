**Updated By:** John Muinde
**Tracking Period:** 15th June – 19th June 2026

---

1. Summary of Key Priorities

Completed This Period

**T-0 Network**
Collection and payout flows activated end-to-end on the T-0 sandbox; race conditions across all state drivers resolved; secure ops dashboard API deployed with HMAC isolation; infrastructure hardened with PrivateLink and egress firewall

**Lightning — IBEX Hub M2M Auth Migration**
Deprecated personal email/password credential replaced with OAuth2 client credentials flow; tokens proactively cached and refreshed; hardcoded employee debug endpoint removed

**Chains — BSC Full Chain Wire-Up**
Binance Smart Chain fully wired across payins, payouts, refunds, live monitoring, and payment links — identical behaviour to all other EVM chains

In Progress

**Postman Collection**
Syncing with current API state: request signing, new settlement endpoints, and admin transaction tools

---

2. T-0 Network

**T-0 Network**
Collection and payout flows activated end-to-end; both sandbox tasks complete (intent_success, intent_reject)
IVMS101 travel rule compliance unblocked; nationalIdentifier switched from MISC (0) to TXID (6) — proto3 default was causing validation rejections
Active pay-in corridor probed across USD, GBP, EUR, KES — first corridor with live quotes used automatically
All 5 payout sandbox scenarios wired via travelRuleData and triggerable from the deploy-script menu without restart
Bank account details stored in metadata.payInDetails and appended to the transaction timeline
Every transaction outcome stored regardless of path — collection and payout failure branches previously returned with no DB record
Timeline entries added for all events: payout failures, AML re-quote approvals and rejections, every monitor poll, Kotani withdrawal, terminal Kotani callbacks
Duplicate T-0 callbacks deduplicated for state changes; full payload appended as duplicate_callback_<case> for dispute resolution
Five concurrent callback race conditions resolved across MongoDB, SQLite, and Memory drivers; terminal states now atomically protected via findOneAndUpdate filter
Secure ops dashboard API deployed — reads go directly to MongoDB with read-only credentials; writes require HMAC-SHA256 with timestamp and nonce replay protection
Hard ceilings enforced at API layer before any T-0 or Kotani call: $500 per payment, 20 writes per hour
Atlas migrated from public IP rotation to PrivateLink VPC NICs; /etc/hosts entry persisted on every deploy
Egress firewall locks outbound to T-0, Kotani, and MongoDB only — cron-based IP rotation and MongoDB watchdog decommissioned
Three transitive CVEs patched via package overrides (form-data, defu, diff); CI deploy now aborts on any high or critical finding

---

Full Breakdown

Completed This Period

**Lightning — IBEX Hub M2M Auth Migration**
Deprecated /auth/signin, /auth/refresh-access-token, /auth/revoke-refresh-token removed
Token acquisition via Auth0 token endpoint using IBEX_HUB_CLIENT_ID, IBEX_HUB_CLIENT_SECRET, IBEX_HUB_OAUTH_DOMAIN, IBEX_HUB_AUDIENCE
Tokens cached with 5-minute early-expiry buffer; proactive refresh every 55 minutes
Hardcoded brian@kotanipay.com debug endpoint removed from auth controller
Webhook fall-through fixed — all unrecognised events were previously classified as unknown
Remove from environment: IBEX_HUB_EMAIL, IBEX_HUB_PASSWORD

**Chains — BSC Full Chain Wire-Up**
Withdraw seed: BINANCE USDC and USDT added with shared EVM escrow address
WebSocket listener: BINANCE added to WS_SUPPORTED_CHAINS; escrow address resolution wired
Offramp: getEscrowAddress, processIntegratedWalletOfframp, and executeRefund all route BINANCE through EVM path
Offramp repository: BINANCE added to pending and cancelled chain filter queries
Deposit onchain: BINANCE maps to evm_key.celo consistent with BASE and ARBITRUM
Payment links: BINANCE → 'bsc' added to both payin and payout network maps
Wallet diagnostics: BINANCE, LISK, OPTIMISM, FUSE added to EVM normalisation and token amount resolution

In Progress

**Postman Collection**
Syncing with current API state: request signing, new settlement endpoints, and admin transaction tools

**T-0 — Production Deployment**
PrivateLink port 27017 pending Atlas cluster update; ready to deploy once Atlas finishes provisioning the connection
